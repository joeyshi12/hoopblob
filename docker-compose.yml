services:
  build:
    build: .
    volumes:
      - .:/game
      - build_cache:/game/build
    command: ["bash", "-c", "make -j$(nproc) LIBBUTANO=/opt/butano PYTHON=python3"]

volumes:
  build_cache:
